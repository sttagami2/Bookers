<% @users.each do |user| %>
    <tr>
      <td><%= user.profile_image_id %></td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user_params) %></td>           <%# クリックされたらshow画面に移動 %>
    </tr>
  <% end %>